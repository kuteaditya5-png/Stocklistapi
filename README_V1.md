# StockLens AI v1.0
Deploy these three changed API files: `api/index.py`, `api/dashboard.py`, `api/monthly_ranker.py`.

Then open Strategy Lab and press **Run v1.0 Ranking Test**.

PASS requires >=6 unseen months, positive average excess return vs NIFTY, and >50% of unseen months beating NIFTY.
